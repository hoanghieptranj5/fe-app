{
  "routes": [
    {
      "route": "/*",
      "rewrite": "/index.html"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/static/*.*", "/assets/*.{js,css,png,jpg,gif,svg,woff,woff2}"]
  }
}